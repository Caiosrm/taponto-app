import React from 'react'
import { IPerfilScreenProps } from './types'
import TopBar from '../../Common/TopBar'

export const PerfilScreen = (props:IPerfilScreenProps) => {
  return (
    <TopBar/>
  )
}
