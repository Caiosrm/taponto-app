import React from "react"
import { Box, Center, Heading, Input } from "native-base";

export default function Login() {
    return (
        <Center height="full">

            <Heading>Testando aplication</Heading>
            <Input />
        </Center>
    );
}