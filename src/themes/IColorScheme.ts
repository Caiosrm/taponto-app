export interface IColorScheme {
    light: {
        primary: string;
        secondary: string;
        // outras cores do tema claro
    };
    dark: {
        primary: string;
        secondary: string;
        // outras cores do tema escuro
    };
}